#include <stdio.h>
#include "lesson-14-math.h"

int main() {

    int result = add(3, 4);

    printf("Result = %d\n", result);
    printf("Operations performed = %d\n", operation_count);

    return 0;
}
